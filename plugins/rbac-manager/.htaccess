# Protect direct access to plugin files
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ - [F]
</IfModule>

# Deny direct access to config files
<Files "config.php">
    Require all denied
</Files>
